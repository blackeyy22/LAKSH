const root=document.getElementById('app');
const esc=s=>String(s??'').replace(/[&<>'"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','"':'&quot;'}[m]));
let csrfToken='';
async function csrf(){if(csrfToken)return csrfToken;const r=await fetch('/api/csrf');const d=await r.json();csrfToken=d.token||'';return csrfToken}
async function api(url,opts={}){opts.headers={...(opts.headers||{})};if((opts.method||'GET').toUpperCase()!=='GET'){const t=await csrf();if(t)opts.headers['X-CSRF-Token']=t}const r=await fetch(url,opts);if(r.status===401){location.href='/login';return null}const d=await r.json();if(!r.ok)throw new Error(d.error||'Request failed');return d}
function product(p){return `<article class="product"><img src="${esc(p.image_url)}" alt="${esc(p.name)}"><div class="product-body"><span class="badge">${esc(p.badge||'FEATURED')}</span><h3>${esc(p.name)}</h3><p>${esc(p.description||'')}</p><button class="btn ghost small" onclick="showProduct('${esc(p.name).replace(/'/g,'&#039;')}')">Explore product</button></div></article>`}
function onboarding(){return `<div class="tour-backdrop" id="tour"><div class="tour-card"><span class="eyebrow">WELCOME TO YOUR MEMBER SPACE</span><h2>Everything you need, in one place.</h2><p>Use your referral code, follow your progress toward the milestone, understand your Auto Pool position after qualification, and discover new store products.</p><div class="tour-points"><div><b>01</b><span>Activate your membership and get your personal referral link.</span></div><div><b>02</b><span>Reach 5 verified direct referrals.</span></div><div><b>03</b><span>Enter the shared 5-wide Auto Pool and follow your three rewarded levels.</span></div></div><button class="btn full" onclick="finishTour()">Show me my dashboard</button><button class="text-button" onclick="finishTour()">Skip for now</button></div></div>`}
function stageCard(label,filled,target){const pct=Math.min(100,Math.round(filled/target*100));return `<div class="pool-stage-card ${filled>=target?'complete':''}"><div><span>${label}</span><strong>${filled}/${target}</strong></div><div class="progress"><i style="width:${pct}%"></i></div><small>${filled>=target?'Stage complete':'Positions filled at this level'}</small></div>`}
function addContactControl(){if(document.getElementById('memberContactButton'))return;const button=document.createElement('button');button.id='memberContactButton';button.className='btn contact-float';button.textContent='Contact us';const panel=document.createElement('section');panel.id='memberContact';panel.className='contact-section';panel.innerHTML='<div class="contact-panel"><span class="eyebrow">CONTACT US</span><h2>We are here to help.</h2><p>Email: <a href="mailto:lakshtara250@gmail.com">lakshtara250@gmail.com</a><br>Phone: <a href="tel:+918779790236">+91 877 979 0236</a><br>78 Business Park, Shop no 67, near Bhandup Station Road, Bhandup West, Mumbai - 400078</p><a class="btn" href="mailto:lakshtara250@gmail.com">Contact Laksh</a></div>';document.body.append(button,panel);button.onclick=()=>panel.scrollIntoView({behavior:'smooth',block:'start'})}
function addPasswordControl(){const actions=document.querySelector('.app-top .top-actions');if(!actions||document.getElementById('memberPasswordButton'))return;const button=document.createElement('button');button.id='memberPasswordButton';button.className='btn ghost small';button.textContent='Change password';const panel=document.createElement('form');panel.id='memberPasswordPanel';panel.className='member-password-panel';panel.hidden=true;panel.innerHTML='<div class="member-password-title"><strong>Change your password</strong><button type="button" class="member-password-close" aria-label="Close">&times;</button></div><label>Current password<input name="currentPassword" type="password" required autocomplete="current-password"></label><label>New password<input name="newPassword" type="password" minlength="8" required autocomplete="new-password"></label><button class="btn small full">Save password</button><p class="form-msg" id="memberPasswordMsg"></p>';actions.parentElement.append(panel);button.onclick=()=>{panel.hidden=!panel.hidden;if(!panel.hidden)panel.querySelector('input').focus()};panel.querySelector('.member-password-close').onclick=()=>{panel.hidden=true};panel.addEventListener('submit',async e=>{e.preventDefault();const message=panel.querySelector('#memberPasswordMsg');try{await api('/api/me/password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(panel)))});message.textContent='Password changed successfully.';message.className='form-msg success';panel.reset();}catch(error){message.textContent=error.message;message.className='form-msg error'}});actions.prepend(button)}
function autoPoolInfoSection(autoPool){if(!autoPool?.active)return `<section class="section compact-section auto-pool-preview" id="auto-pool"><div class="section-head" style="padding:0"><div><span class="eyebrow">AUTO POOL</span><h2>Your Auto Pool view unlocks after 5 verified referrals.</h2><p class="small-note">The Auto Pool is separate from your personal referral relationship. When you complete 5 verified direct referrals, you enter a shared matrix that is 5 positions wide and continues deeper as members qualify.</p></div></div><div class="pool-flow-preview"><div class="pool-flow-step"><span>01</span><strong>Complete 5</strong><small>Verified direct referrals</small></div><div class="pool-flow-arrow">→</div><div class="pool-flow-step"><span>02</span><strong>Enter Auto Pool</strong><small>Receive your global position</small></div><div class="pool-flow-arrow">→</div><div class="pool-flow-step"><span>03</span><strong>5 wide</strong><small>Unlimited depth</small></div></div></section>`;
return `<section class="section compact-section auto-pool-section" id="auto-pool"><div class="section-head" style="padding:0"><div><span class="eyebrow">AUTO POOL</span><h2>Your position in the shared matrix</h2><p class="small-note">You qualified after completing 5 verified direct referrals. Your personal sponsor relationship remains separate; the Auto Pool position below is based on when you joined the global matrix.</p></div><div class="pool-position-badge"><span>YOUR POSITION</span><strong>${autoPool.position}</strong></div></div><div class="pool-stage-grid">${stageCard('STAGE 1',autoPool.stageCounts?.[1]||0,5)}${stageCard('STAGE 2',autoPool.stageCounts?.[2]||0,25)}${stageCard('STAGE 3',autoPool.stageCounts?.[3]||0,125)}</div><div class="pool-copy"><div><strong>5 positions wide</strong><span>Each matrix position can have up to 5 positions directly below it.</span></div><div><strong>Unlimited depth</strong><span>The structure can continue below the three rewarded stages.</span></div><div><strong>Private member view</strong><span>You see only your own position and the structure below it using placeholders, not member names.</span></div></div><div class="pool-graph-card"><div class="pool-graph-top"><div><span class="eyebrow">YOUR AUTO POOL VIEW</span><h3>Only your branch below this position is visible.</h3></div><div class="pool-graph-tools"><button onclick="memberPoolZoom(1.12)">+</button><button onclick="memberPoolZoom(.89)">−</button><button onclick="memberPoolReset()">Fit</button></div></div><div class="pool-canvas" id="memberPoolCanvas"><svg id="memberPoolSvg" class="pool-svg"><g id="memberPoolWorld"><g id="memberPoolLinks"></g><g id="memberPoolNodes"></g></g></svg><div class="graph-hint">WHEEL = ZOOM&nbsp;&nbsp; • &nbsp;&nbsp;DRAG = PAN</div></div></div></section>`}
function buildMemberPoolGraph(data){const svg=document.getElementById('memberPoolSvg'),world=document.getElementById('memberPoolWorld'),links=document.getElementById('memberPoolLinks'),nodesG=document.getElementById('memberPoolNodes');if(!svg||!data?.active)return;const actual=new Map((data.nodes||[]).map(n=>[Number(n.position),n]));const rootPos=Number(data.position);const expected=[{position:rootPos,parentPosition:null,slot:null,depth:0,isRoot:true}];let current=[rootPos];for(let depth=1;depth<=3;depth++){const next=[];for(const p of current){for(let slot=1;slot<=5;slot++){const childPos=5*p+slot-4;next.push(childPos);expected.push({position:childPos,parentPosition:p,slot,depth,isRoot:false});}}current=next;}const nodes=expected.map(n=>({...n,actual:actual.get(n.position)||null,x:0,y:0,w:n.depth===0?150:112,h:n.depth===0?68:54}));const grouped=[0,1,2,3].map(d=>nodes.filter(n=>n.depth===d));const maxCount=Math.max(...grouped.map(r=>r.length),1);const spacing=132;const width=Math.max(1050,maxCount*spacing+220),height=580;grouped.forEach((row,d)=>{const rowWidth=(row.length-1)*spacing;const startX=(width-rowWidth)/2;row.forEach((n,i)=>{n.x=startX+i*spacing-n.w/2;n.y=30+d*132;});});const pos=new Map(nodes.map(n=>[n.position,n]));links.innerHTML=nodes.filter(n=>n.parentPosition&&pos.has(n.parentPosition)).map(n=>{const p=pos.get(n.parentPosition);const childExists=!!n.actual;return `<path class="pool-link ${childExists?'filled':'ghost'}" d="M ${p.x+p.w/2} ${p.y+p.h} C ${p.x+p.w/2} ${p.y+p.h+34}, ${n.x+n.w/2} ${n.y-34}, ${n.x+n.w/2} ${n.y}"/>`;}).join('');nodesG.innerHTML=nodes.map(n=>{const label=n.isRoot?'YOU':`POOL ${String(n.position).padStart(3,'0')}`;const status=n.isRoot?'Your position':(n.actual?'Occupied':'Available');return `<g class="pool-node ${n.isRoot?'is-root':''} ${n.actual?'occupied':'empty'}" data-pos="${n.position}" transform="translate(${n.x},${n.y})"><rect width="${n.w}" height="${n.h}" rx="14"></rect><text class="pool-node-label" x="${n.w/2}" y="${n.isRoot?28:23}" text-anchor="middle">${label}</text><text class="pool-node-meta" x="${n.w/2}" y="${n.isRoot?47:41}" text-anchor="middle">${status}</text></g>`}).join('');svg.setAttribute('viewBox',`0 0 ${width} ${height}`);svg.style.width=`${width}px`;svg.style.minWidth=`${width}px`;svg.style.height=`${height}px`;memberPoolState.nodes=nodes;memberPoolState.world=world;memberPoolApply();attachMemberPoolEvents(svg);}
const memberPoolState={scale:.78,tx:0,ty:0,nodes:[],world:null,pan:null};
function memberPoolApply(){if(memberPoolState.world)memberPoolState.world.setAttribute('transform',`translate(${memberPoolState.tx} ${memberPoolState.ty}) scale(${memberPoolState.scale})`)}
function attachMemberPoolEvents(svg){svg.onwheel=e=>{e.preventDefault();memberPoolZoom(e.deltaY<0?1.1:.9)};svg.onmousedown=e=>{if(e.target.closest('.pool-node'))return;memberPoolState.pan={x:e.clientX,y:e.clientY,tx:memberPoolState.tx,ty:memberPoolState.ty};};svg.onmousemove=e=>{if(!memberPoolState.pan)return;memberPoolState.tx=memberPoolState.pan.tx+e.clientX-memberPoolState.pan.x;memberPoolState.ty=memberPoolState.pan.ty+e.clientY-memberPoolState.pan.y;memberPoolApply();};svg.onmouseup=()=>memberPoolState.pan=null;svg.onmouseleave=()=>memberPoolState.pan=null;}
window.memberPoolZoom=f=>{memberPoolState.scale=Math.min(8,Math.max(.12,memberPoolState.scale*f));memberPoolApply()};window.memberPoolReset=()=>{memberPoolState.scale=.78;memberPoolState.tx=0;memberPoolState.ty=0;memberPoolApply()};
async function load(){try{const d=await api('/api/me');if(!d)return;const pct=Math.min(100,Math.round(d.referral.verified/d.referral.required*100)),remaining=Math.max(0,d.referral.required-d.referral.verified),active=d.user.membershipPaid,pendingReferral=esc(sessionStorage.getItem('pendingReferralCode')||'');const refCode=esc(d.user.referralCode||''),refLink=esc(d.referral.link||'');root.innerHTML=`<div class="app-shell"><header class="topbar app-top"><a class="brand" href="/">LAKSH</a><div class="top-actions"><button class="lang-switch" onclick="toggleHindi()" aria-label="Toggle Hindi">EN / हिन्दी</button><span class="member-status ${active?'live':''}">${active?'MEMBER ACTIVE':'MEMBERSHIP INACTIVE'}</span>${active?'<button class="btn ghost small" onclick="logout()">Log out</button>':'<a class="btn small" href="/payment">Activate ₹250</a>'}</div></header><main class="app-main"><section class="dash-intro"><div><span class="eyebrow">YOUR MEMBER SPACE</span><h1>Welcome back, ${esc(d.user.name)}.</h1><p>${active?'Your progress, referral tools and reward journey in one place.':'Activate your membership to unlock your full member experience.'}</p></div>${active?`<div class="intro-code"><span>YOUR CODE</span><strong>${refCode}</strong><button class="btn small" onclick="copyText('${refLink}')">Share referral</button></div>`:`<div class="intro-activate"><div><span class="eyebrow">GET STARTED</span><strong>Membership ₹250</strong><p>${d.payment?.status==='PENDING_REVIEW'?'Payment proof submitted — waiting for admin approval.':'Pay by QR and submit your payment proof.'}</p></div><a class="btn" href="/payment">${d.payment?.status==='PENDING_REVIEW'?'View payment status':'Activate membership'}</a></div>`}</section>${!active?`<section class="activation-note"><div><span class="eyebrow">ONE STEP TO UNLOCK</span><h2>Activate your membership</h2><p>Pay ₹250 by QR, upload your proof, and our team will review it within up to 2 hours. Your own referral code and link are generated after approval.</p></div><a class="btn" href="/payment">Continue to payment</a></section><section class="section compact-section"><div class="section-head" style="padding:0"><div><span class="eyebrow">REFERRAL CODE</span><h2>Were you invited?</h2><p class="small-note">You can enter a friend's active referral code after login. This is optional and can only be attached before membership activation.</p></div></div><form class="attach-referral-form" onsubmit="attachReferral(event)"><input name="referralCode" value="${pendingReferral}" placeholder="RT-XXXXXXXX" maxlength="64" required><button class="btn">Add referral code</button><p id="attachMsg" class="form-msg"></p></form></section>`:''}${d.payment?.status==='PENDING_REVIEW'?`<section class="payment-pending-banner"><strong>Payment proof submitted</strong><span>Our team is reviewing it. Please allow up to 2 hours. You can continue to use your dashboard while it is under review.</span></section>`:d.payment?.status==='REJECTED'?`<section class="payment-pending-banner"><strong>Payment proof needs attention</strong><span>The previous proof was rejected. Please submit a clearer proof on the payment page.</span><a class="btn small" href="/payment">Submit again</a></section>`:''}<section class="dash-grid" id="referral"><div class="panel progress-panel"><div class="panel-kicker"><span>REFERRAL PROGRESS</span><b>${d.referral.verified}/${d.referral.required}</b></div><h2>${remaining?`${remaining} more verified referral${remaining===1?'':'s'} to unlock your reward.`:'Milestone complete — your reward is unlocked.'}</h2><div class="progress big"><i style="width:${pct}%"></i></div><div class="mini-row"><span>${d.referral.verified} verified</span><span>${pct}% complete</span></div><div class="ref-actions">${active?`<button class="btn" onclick="copyText('${refLink}')">Copy referral link</button><button class="btn ghost" onclick="copyText('${refCode}')">Copy code</button><button class="btn ghost" onclick="shareIt('${refLink}')">Share</button>`:`<a class="btn" href="/payment">Activate membership to get your referral code</a>`}</div></div><div class="panel network-panel"><span class="eyebrow">YOUR NETWORK</span><div class="network-number">${d.network.count}</div><h3>members in your network</h3><p>Only the total is shown here for member privacy. Your admin can see the complete referral hierarchy.</p></div></section>${autoPoolInfoSection(d.autoPool)}<section class="section compact-section" id="reward"><div class="section-head" style="padding:0"><div><span class="eyebrow">YOUR REWARD</span><h2>${esc(d.reward.name||'Configured reward')}</h2></div><span class="status-pill ${d.reward.unlocked?'success':''}">${d.reward.unlocked?'UNLOCKED':'IN PROGRESS'}</span></div><div class="reward-large"><div class="reward-icon reward-image"><img src="${esc(d.reward.image_url||'/assets/images/reward-mic-speaker.svg')}" alt="${esc(d.reward.name||'Reward')}"></div><div><h3>${esc(d.reward.name||'Configured reward')}</h3><p>${esc(d.reward.description||'Complete the referral milestone to unlock this reward.')}</p><strong>${d.referral.required} verified referrals required</strong></div><div class="reward-progress"><span>${d.referral.verified}/${d.referral.required}</span><div class="progress"><i style="width:${pct}%"></i></div></div></div></section><section class="section compact-section" id="store"><div class="section-head" style="padding:0"><div><span class="eyebrow">FROM OUR STORE</span><h2>Featured products</h2></div><span class="small-note">Discover what’s new in the store.</span></div><div class="product-grid">${d.products.map(product).join('')}</div></section><section class="section compact-section"><div class="section-head" style="padding:0"><div><span class="eyebrow">RECENT ACTIVITY</span><h2>Your latest updates</h2></div></div><div class="activity-list">${d.activity.length?d.activity.map(a=>`<div class="activity-item"><div class="activity-mark"></div><div><strong>${esc(a.title)}</strong><p>${esc(a.detail||'')}</p><small>${new Date(a.created_at).toLocaleString()}</small></div></div>`).join(''):'<p class="small-note">Your activity will appear here as you use the platform.</p>'}</div></section><section class="how-strip"><div><span class="eyebrow">HOW IT WORKS</span><h2>Simple, clear, transparent.</h2></div><div class="how-mini"><div><b>01</b><strong>Join</strong><span>Create your member account.</span></div><div><b>02</b><strong>Activate</strong><span>Get your personal code.</span></div><div><b>03</b><strong>Share</strong><span>Send your link to friends.</span></div><div><b>04</b><strong>Reach 5</strong><span>Enter the Auto Pool when the milestone is verified.</span></div></div></section></main><nav class="mobile-nav"><a class="active" href="#referral">Home</a><a href="#referral">Referral</a><a href="#auto-pool">Auto Pool</a><a href="#reward">Rewards</a><a href="#store">Store</a><button onclick="logout()">Log out</button></nav>${(!d.user.onboardingComplete&&active)?onboarding():''}</div>`;if(d.autoPool?.active)requestAnimationFrame(()=>buildMemberPoolGraph(d.autoPool));}catch(e){root.innerHTML=`<div class="auth-shell"><section class="auth-card"><h1>Something went wrong.</h1><p>${esc(e.message)}</p><a class="btn" href="/login">Return to sign in</a></section></div>`}}
window.copyText=async t=>{try{await navigator.clipboard.writeText(t);toast('Copied to clipboard')}catch(e){prompt('Copy this',t)}};
window.shareIt=async t=>{if(navigator.share)await navigator.share({title:'Join me on Laksh',text:'Here is my referral link',url:t});else copyText(t)};
window.showProduct=name=>alert(`${name}\n\nThis is a promotional store showcase. Product purchasing is not enabled in this version.`);
window.attachReferral=async e=>{e.preventDefault();const form=e.currentTarget,msgEl=document.getElementById('attachMsg'),input=form.querySelector('input'),button=form.querySelector('button');msgEl.textContent='';try{const d=await api('/api/me/attach-referral',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form)))});if(d?.ok){sessionStorage.removeItem('pendingReferralCode');msgEl.textContent=`Referral linked to ${d.referrer}. Complete membership activation to verify it.`;msgEl.className='form-msg success';input.disabled=true;button.disabled=true;}}catch(err){msgEl.textContent=err.message;msgEl.className='form-msg error'}};
window.finishTour=async()=>{await api('/api/me/onboarding',{method:'POST',headers:{'Content-Type':'application/json'},body:'{}'});document.getElementById('tour')?.remove()};
window.toast=t=>{const el=document.createElement('div');el.className='toast';el.textContent=t;document.body.appendChild(el);setTimeout(()=>el.remove(),1800)};
window.logout=async()=>{try{await api('/api/logout',{method:'POST'})}catch{}location.href='/'};

const HI_MAP={
  'YOUR MEMBER SPACE':'आपका सदस्य स्थान','Welcome back,':'वापसी पर स्वागत है,','MEMBERSHIP INACTIVE':'सदस्यता सक्रिय नहीं','MEMBER ACTIVE':'सदस्य सक्रिय','Activate ₹250':'₹250 सक्रिय करें','REFERRAL PROGRESS':'रेफरल प्रगति','YOUR NETWORK':'आपका नेटवर्क','members in your network':'आपके नेटवर्क में सदस्य','YOUR REWARD':'आपका इनाम','FROM OUR STORE':'हमारी दुकान से','Featured products':'विशेष उत्पाद','RECENT ACTIVITY':'हाल की गतिविधि','HOW IT WORKS':'यह कैसे काम करता है','Simple, clear, transparent.':'सरल, स्पष्ट और पारदर्शी।','Auto Pool':'ऑटो पूल','REACH 5':'5 पूरा करें','Copy referral link':'रेफरल लिंक कॉपी करें','Copy code':'कोड कॉपी करें','Share':'शेयर करें','Store':'दुकान','Rewards':'इनाम','Log out':'लॉग आउट','YOUR CODE':'आपका कोड','GET STARTED':'शुरू करें','ONE STEP TO UNLOCK':'सक्रिय करने के लिए एक कदम','Continue to payment':'भुगतान पर जाएँ','Activate membership':'सदस्यता सक्रिय करें','Payment proof submitted':'भुगतान प्रमाण जमा हो गया','Our team is reviewing it. Please allow up to 2 hours.':'हमारी टीम इसकी समीक्षा कर रही है। कृपया 2 घंटे तक प्रतीक्षा करें।'
};
const EN_MAP=Object.fromEntries(Object.entries(HI_MAP).map(([a,b])=>[b,a]));
function walkTranslate(map){const walker=document.createTreeWalker(document.body,NodeFilter.SHOW_TEXT);const nodes=[];while(walker.nextNode())nodes.push(walker.currentNode);for(const n of nodes){const t=n.nodeValue.trim();if(map[t])n.nodeValue=n.nodeValue.replace(t,map[t]);}}
window.toggleHindi=()=>{const hi=localStorage.getItem('rt_lang')!=='hi';localStorage.setItem('rt_lang',hi?'hi':'en');walkTranslate(hi?HI_MAP:EN_MAP);document.documentElement.lang=hi?'hi':'en';};
function applySavedLanguage(){if(localStorage.getItem('rt_lang')==='hi'){walkTranslate(HI_MAP);document.documentElement.lang='hi';}}
load().then(()=>{addPasswordControl();addContactControl()});
setTimeout(applySavedLanguage,80);
